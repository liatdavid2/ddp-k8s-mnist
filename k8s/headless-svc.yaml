apiVersion: v1
kind: Service
metadata:
  name: ddp-master-svc
spec:
  clusterIP: None

  selector:
    app: ddp
    role: master

  ports:
    - name: rendezvous
      port: 29500
      targetPort: 29500