apiVersion: batch/v1
kind: Job
metadata:
  name: ddp-master
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app: ddp
        role: master
    spec:
      restartPolicy: Never
      containers:
        - name: master
          image: ddp-mnist:0.1
          imagePullPolicy: Never

          command: ["torchrun"]

          args:
            - "--nproc_per_node=1"
            - "--nnodes=2"
            - "--node_rank=0"
            - "--rdzv_backend=c10d"
            - "--rdzv_endpoint=ddp-master-svc:29500"
            - "--rdzv_id=mnist-ddp"
            - "/app/src/train.py"

          env:
            - name: MASTER_ADDR
              value: "ddp-master-svc"
            - name: MASTER_PORT
              value: "29500"
            - name: NCCL_SOCKET_IFNAME
              value: "eth0"
            - name: GLOO_SOCKET_IFNAME
              value: "eth0"